org: studentkasia
app: lens-api
service: lens-api

provider:
  name: aws
  runtime: nodejs20.x
  region: eu-north-1
  deploymentMethod: direct
  iam:
    role: arn:aws:iam::796973483684:role/LambdaDynamoDbRole
  httpApi:
    cors: true

functions:
  CreateContinent:
    handler: functions/addContinent/index.handler
    events:
      - httpApi:
          path: /continent
          method: post

  CreatePlace:
    handler: functions/addPlace/index.handler
    events:
      - httpApi:
          path: /place
          method: post

  GetContinent:
    handler: functions/getContinent/index.handler
    events:
      - httpApi:
          path: /continent/{continent}
          method: get

resources:
  Resources:
    TravelLensTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: TravelLensTable
        AttributeDefinitions:
          - AttributeName: PK
            AttributeType: S
          - AttributeName: SK
            AttributeType: S
        KeySchema:
          - AttributeName: PK
            KeyType: HASH
          - AttributeName: SK
            KeyType: RANGE
        BillingMode: PAY_PER_REQUEST
